document.getElementById("export-data").addEventListener("click",()=>{const saved=JSON.parse(localStorage.getItem("mis-gastos-v1")||'{"expenses":[],"incomes":[]}'),q=v=>`"${String(v??"").replaceAll('"','""')}"`,movements=[...saved.incomes.map(x=>["Ingreso",x.date,x.type,x.amount,x.comment]),...saved.expenses.map(x=>["Gasto",x.date,x.category,x.amount,x.comment])].sort((a,b)=>b[1].localeCompare(a[1])),rows=[["Tipo","Fecha","Categoría / entrada","Importe","Comentario"],...movements],blob=new Blob(["\ufeff"+rows.map(row=>row.map(q).join(";")).join("\n")],{type:"text/csv;charset=utf-8"}),url=URL.createObjectURL(blob),a=document.createElement("a");a.href=url;a.download=`gastito-de-blas-${new Date().toISOString().slice(0,10)}.csv`;a.click();URL.revokeObjectURL(url)});
