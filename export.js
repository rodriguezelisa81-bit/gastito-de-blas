document.getElementById("export-data").addEventListener("click",()=>{const saved=JSON.parse(localStorage.getItem("mis-gastos-v1")||'{"expenses":[]}'),q=value=>`"${String(value??"").replaceAll('"','""')}"`,rows=[["Fecha","Categoría","Importe","Comentario"],...saved.expenses.sort((a,b)=>b.date.localeCompare(a.date)).map(x=>[x.date,x.category,x.amount,x.comment])],blob=new Blob(["\ufeff"+rows.map(row=>row.map(q).join(";")).join("\n")],{type:"text/csv;charset=utf-8"}),url=URL.createObjectURL(blob),a=document.createElement("a");a.href=url;a.download=`gastito-de-blas-${new Date().toISOString().slice(0,10)}.csv`;a.click();URL.revokeObjectURL(url)});
